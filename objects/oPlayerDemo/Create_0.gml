//This is an example of a character moving only when the music beats

//First set of variables, move_requested if the player input a move, and
//move_enabled if the object is allowed to move
move_requested = false;
move_enabled = false;

xTo = x;
yTo = y;

depth -= 10;